{% extends 'base.html.twig' %}
{% block title %}{{ post.title }}{% endblock %}
{% block body %}
    <p>ID {{ id }} {{ title }}</p>
    <h3>{{ post.title }}</h3>
    <p>{{ post.text }}</p>

    <p>{{ post.date|date('H:i:s') }}</p>
    <p>{{ 12300.6562|price }}</p>
    <p>{{ 500|test }}</p>

{% endblock %}